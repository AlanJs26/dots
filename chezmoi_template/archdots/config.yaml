# here is all files/folders that are re-added when the sync command is executed
# additional files can be installed with "archdots file add"
chezmoi:
  - ~/.config/archdots

# here is listed all packages that must be installed
# pkgs:
#   custom:
#     - custom_package1
#     - custom_package2
#   pacman:
#     - package1
#     - package2

# ignored packages by package manager.
# accepted formats:
# - exact package name: "package1"
# - regex pattern: "re:^lib.*"
# - glob pattern: "glob:python*"
# ignored_pkgs:
#   custom:
#     - old_package
#   pacman:
#     - re:^lib32-
#     - glob:linux-headers*


